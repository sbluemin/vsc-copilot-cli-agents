# Extension Development Test Workspace

This folder is used as a test workspace when debugging the VS Code extension.

When you press F5 to launch the Extension Development Host, this workspace is opened for testing extension features.
